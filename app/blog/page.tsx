/* eslint-disable @next/next/no-img-element */
import type {Metadata} from "next"; import Link from "next/link"; import {Breadcrumb,Footer,Header} from "../ui"; import {articles} from "./articles";
export const metadata:Metadata={title:"Blog de Dermatologia | Clínica QARA",description:"Informação médica revisada sobre pele, cabelos, unhas, câncer da pele e doenças inflamatórias.",alternates:{canonical:"/blog"}};
export default function Blog(){const cats=[...new Set(articles.map(a=>a.category))];return <><Header/><main id="conteudo"><Breadcrumb>Conteúdo médico</Breadcrumb><section className="blog-hero"><div className="shell"><p className="kicker">Orientação baseada em evidências</p><h1>Entenda os sinais, os exames e as opções de tratamento.</h1><p className="lead">Artigos escritos por dermatologistas da Clínica QARA, com autoria, referências e linguagem clara para pacientes.</p></div></section><section className="shell blog-index"><nav aria-label="Navegar pelos temas do blog"><a href="#artigos">Todos os artigos</a>{cats.map(c=><a href={`#${c.toLowerCase().replaceAll(" ","-")}`} key={c}>{c}</a>)}</nav><div className="blog-grid" id="artigos">{articles.map((a,i)=><article className={i===0?"featured":""} id={articles.findIndex(x=>x.category===a.category)===i?a.category.toLowerCase().replaceAll(" ","-"):undefined} key={a.slug}><Link className="blog-image" href={`/blog/${a.slug}`}><img src={a.image} alt={a.title} width={1400} height={788} decoding="async" fetchPriority={i===0?"high":"auto"} loading={i===0?"eager":"lazy"}/></Link><div><span>{a.category} · {a.readTime}</span><h2><Link href={`/blog/${a.slug}`}>{a.title}</Link></h2><p>{a.description}</p><Link href={`/blog/${a.slug}`}>Ler o artigo completo →</Link></div></article>)}</div></section></main><Footer/></>}
